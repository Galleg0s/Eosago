import { createSelector } from 'reselect';

export const hoSourceSelector = state => state.runtime.hoSource;
export const userRegionIdSelector = state => state.runtime.userRegionId;
export const aff_sub3Selector = state => state.runtime.aff_sub3;
