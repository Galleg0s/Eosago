import {State} from '@BUNDLES/InsuranceBundle/Resources/static/eosago/scripts/form/types';

export const isAgreementModalVisibleSelector = (state: State) => state.modals.isAgreementModalVisible;
export const isSubscribeModalVisibleSelector = (state: State) => state.modals.isSubscribeModalVisible;
